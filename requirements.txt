moviepy==1.0.3
edge-tts  # Optional fallback for TTS
requests
google-api-python-client
google-generativeai
google-cloud-texttospeech
google-auth-oauthlib>=0.5.0  # For YouTube OAuth flow
google-auth-httplib2>=0.1.0  # For OAuth HTTP transport
oauth2client
imageio[ffmpeg]
Pillow>=10.0.0
python-dotenv
newspaper3k
lxml_html_clean  # Required for newspaper3k with newer lxml versions
nltk  # Required for newspaper3k tokenization
whisper-timestamped  # Optional: For word-level timing extraction in captions
torch  # Required for whisper-timestamped

